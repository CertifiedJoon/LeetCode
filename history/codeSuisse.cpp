#include <bits/stdc++.h>
using namespace std;
class Solution{
public:
  vector<int> leastSums (vector<int> nums){
    vector<int> sorted_num (len(nums));
    copy(nums.begin(), nums.end(), sorted_num.begin());


  }

  void find_combination(int starting_index, vector<int> sorted_num, vector<int> &shortest_combination, vector<int> current_combination){

  }

  void copy(nums.begin)
};
